# 🔍 Telegram Work Analyzer

Анализирует твои Telegram чаты за последний месяц и генерирует:
- **SOP документы** — готовые процессы для делегирования
- **Рекомендации** — что оптимизировать в коммуникации
- **Метрики** — сколько времени на операционку vs стратегию
- **Action план** — конкретные шаги для улучшения

## 🚀 Быстрый старт

### 1. Установка

```bash
cd tg_work_analyzer
pip install -r requirements.txt
```

### 2. Настройка переменных

Создай `.env` файл:

```env
TELEGRAM_API_ID=твой_api_id
TELEGRAM_API_HASH=твой_api_hash
ANTHROPIC_API_KEY=твой_ключ
```

### 3. Запуск

```bash
python analyzer.py
```

При первом запуске попросит авторизацию в Telegram (код из приложения).

## 📊 Что получишь

После запуска в папке `reports/` появятся:

```
reports/
├── main_report.md      # Главный отчёт со всеми insights
├── action_plan.md      # Приоритезированный план действий
├── full_analysis.json  # Полный JSON анализа
├── stats.json          # Статистика без текстов сообщений
└── sops/
    ├── SOP_01_*.md     # Готовые SOP документы
    ├── SOP_02_*.md
    └── ...
```

## ⚙️ Настройки

В начале `analyzer.py` можно изменить:

```python
DAYS_TO_ANALYZE = 30        # Период анализа (дней)
MAX_MESSAGES_PER_CHAT = 500 # Макс сообщений с одного чата
MAX_CHATS = 50              # Макс чатов для анализа
```

## 🔒 Приватность

- Сырые тексты сообщений **не сохраняются** в отчётах
- Данные отправляются в Claude API для анализа (Anthropic не хранит данные)
- Сессия Telegram сохраняется локально в `*.session` файле

## 💡 Рекомендации

1. **Запускай раз в месяц** — отслеживай прогресс
2. **Делегируй по SOP** — передавай документы команде
3. **Автоматизируй** — используй идеи из отчёта

---

*Создано для Robert @ ZBS News ecosystem*
